if(1+1 === 2) {
    console.log("정답입니다!")
    } else {
            console.log("틀렸습니다")
    
    }
    // VM494:2 정답입니다!
    // undefined
    if (1 + 1===3) {
        console.log("정답입니다!")
        } else {
            console.log("틀렸습니다")
    }
    // VM583:4 틀렸습니다
    // undefined
    const pw1 = "123"
    // undefined
    const pw2 = "345"
    // undefined
    if (w1 === pw2) {
    alert ("비밀번호가 틀렸습니다")
    }
    // undefined
    if(pw1 === pw2) {
        alert("비밀번호가 틀렸습니다")
    }else {
        alert("예?")
    }